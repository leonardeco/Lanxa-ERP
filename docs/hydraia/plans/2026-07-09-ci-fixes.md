# Plan — CI green: frontend TS + ecdsa CVE

**Spec:** `docs/hydraia/specs/2026-07-09-ci-fixes-design.md`
**Branch:** `fix/ci-frontend-ts-ecdsa`

## Task 1 — Frontend mock typing (fixes TS2493)

**File:** Modify `frontend/src/views/CotizacionesEdit.test.tsx`.

Replace the hoisted mock block (unique anchor):
```ts
const { updateSpy, createSpy } = vi.hoisted(() => ({
  updateSpy: vi.fn(() => Promise.resolve({ data: {} })),
  createSpy: vi.fn(() => Promise.resolve({ data: {} })),
}));
```
with:
```ts
const { updateSpy, createSpy } = vi.hoisted(() => ({
  updateSpy: vi.fn((_id: number, _payload?: unknown) => Promise.resolve({ data: {} })),
  createSpy: vi.fn((_payload?: unknown) => Promise.resolve({ data: {} })),
}));
```

**Verify:** from `frontend/` → `npx tsc --noEmit` exits 0; `npm run test -- CotizacionesEdit` passes.

## Task 2 — pip-audit ignore + PENDIENTES note (fixes ecdsa CVE)

**File:** Modify `.github/workflows/ci.yml`.

Replace (unique anchor, the pip-audit run block):
```yaml
      - name: pip-audit
        run: |
          pip install pip-audit
          pip-audit -r requirements.txt --desc off
```
with:
```yaml
      - name: pip-audit
        run: |
          pip install pip-audit
          # PYSEC-2026-1325 (ecdsa 0.19.2): side-channel en firma ECDSA. Inalcanzable
          # aqui: los JWT se firman con HS256 (HMAC) y python-jose usa el backend
          # cryptography; ecdsa solo se importa para algoritmos ES*. Sin version con
          # fix (0.19.2 es la ultima). Se ignora ese advisory con justificacion.
          pip-audit -r requirements.txt --desc off --ignore-vuln PYSEC-2026-1325
```

**File:** Modify `PENDIENTES.md` — add a 🔵 Nice-to-have bullet:
"**ecdsa / PYSEC-2026-1325**: se ignora en CI (inalcanzable, HS256 + backend
cryptography). Evaluar migrar `python-jose` → `PyJWT` para eliminar la dependencia
`ecdsa` por completo."

**Verify:** from `backend/` → `venv/Scripts/python.exe -m pip-audit -r requirements.txt --desc off --ignore-vuln PYSEC-2026-1325` exits 0.
`grep -c "ignore-vuln PYSEC-2026-1325" .github/workflows/ci.yml` → `1`.
