# ── Web Framework ────────────────────────────────────────
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.52.2
python-multipart==0.0.32

# ── Database ─────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
psycopg2-binary==2.9.12
aiosqlite==0.22.1
alembic==1.19.1

# ── Validation & Serialization ───────────────────────────
pydantic==2.13.4
pydantic-settings==2.15.0
email-validator==2.3.0

# ── Security ─────────────────────────────────────────────
PyJWT==2.13.0  # firma/validacion de JWT (HS256). Reemplaza a python-jose (jul-2026: elimina la dep transitiva ecdsa / PYSEC-2026-1325)
bcrypt==5.0.0  # uso directo (passlib eliminado en jul-2026: sin mantenimiento, bloqueaba bcrypt>=4.1)
slowapi==0.1.10
cryptography==49.0.0

# ── Logging ──────────────────────────────────────────────
structlog==26.1.0

# ── Excel (importador de inventario) ─────────────────────
openpyxl==3.1.5

# ── CORS & Utils ─────────────────────────────────────────
httpx==0.28.1
tzdata==2026.3  # base IANA para ZoneInfo(America/Bogota) — Windows no la trae
